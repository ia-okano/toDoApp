# プロジェクト運用メモ

## カスタムコマンド

### `ultrabuscar [tema]`
ユーザーが `ultrabuscar [tema]`（または「ultrabuscar <トピック>」）と入力したら、以下を実行する:

1. `deep-research` スキルを使って、そのテーマについて**深いWeb調査**（複数ソースのファンアウト検索・出典の突き合わせ・検証）を行う。
   - テーマが曖昧な場合のみ、調査開始前に2〜3個の絞り込み質問をする。
2. 調査結果を**出典付きのレポート**としてMarkdownファイルに保存する。
   - 保存先: リポジトリ直下の `research/` ディレクトリ（無ければ作成）。
   - ファイル名: `research/ultrabuscar_<テーマのスラッグ>_<YYYY-MM-DD>.md`。
3. 保存後、ファイルパストと要点サマリをチャットで返す。

補足: これはユーザーが入力するトリガーフレーズであり、イベント駆動のhookではない（設定は本メモで管理）。

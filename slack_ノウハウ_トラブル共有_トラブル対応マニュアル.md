# 新人研修 開発トラブル対応マニュアル（ドラフト）

> 出典: Slackチャンネル `#ノウハウ_トラブル共有`（C0B4K3VSE3X、`ia2026.slack.com`ワークスペース）
> 収集期間: 2026年5月19日（チャンネル開設）〜6月24日（最終投稿）
> 位置づけ: 本チャンネルは`ia2026`全体チャンネルの5/19投稿「トラブルが多いので、以下のチャンネルに情報を集めていきます」を受けて開設された、**開発演習中のトラブル事例・解決策の集約用**チャンネル。
> **本ファイルはドラフトです。** 番号付きで症状→原因→対処法の形式に整理していますが、実際にトラブル対応マニュアルとして配布・PDF化する前に、各項目の実機での再現性確認、スクリーンショットの追加、表現の統一などの編集を想定しています。
> 各項目末尾の「出典」は、内容の裏付けを取りやすくするための情報源メモです（マニュアル確定版では削除して構いません）。

---

## 目次

1. XAMPP / MySQL 関連トラブル（No.1〜4）
2. SpringBoot / Gradle 関連トラブル（No.5〜9）
3. Git / GitHub 関連トラブル（No.10）
4. ブラウザ表示関連トラブル（No.11）
5. 開発効率化のヒント（トラブル対応ではないが有用な情報）（No.12〜14）
6. 未解決・要フォローアップ事項

---

## 1. XAMPP / MySQL 関連トラブル

### No.1 — XAMPPのMySQLが起動時に落ちる（基本パターン）

**症状**
XAMPPコントロールパネルでMySQLをStartしようとすると、以下のように表示されて起動に失敗する。
```
Attempting to start MySQL app…
Status change detected: running
Status change detected: stopped
Error: MySQL shutdown unexpectedly.
This may be due to a blocked port, missing dependencies,
improper privileges, a crash, or a shutdown by another method.
```

**対処法**
1. 下記サイトの対応手順を実施する。
   `https://jobtech.jp/mysql/4005/`
2. **今後の再発防止のため**、XAMPPを閉じる際は、ウィンドウ右上の×ボタンでいきなり閉じるのではなく、必ずコントロールパネル上でMySQLの「Stop」を押してから閉じるようにする。

*出典: IA_岡野隼、2026-05-19*

---

### No.2 — 上記の対応をしてもMySQLのエラーが解消しない場合

**症状**
No.1の対処法（jobtech.jpの手順）を実施しても、XAMPP起動時にMySQLのエラーが解消しない。

**対処法**
1. XAMPPを完全終了する。
2. `C:\xampp\mysql\data\` フォルダ内にある `ibtmp1` を削除する。
3. XAMPPを再起動する。

*出典: IA_金谷優太、2026-05-22*

---

### No.3 — XAMPP終了のたびに「開けない／毎回初期化される」場合の復旧手順（応急処置・要注意）

**症状**
XAMPPを毎回正しい手順で終了しているにもかかわらず、起動時にMySQLでエラーが発生し、正常に起動できない状態が続く。

**⚠️ 注意**: 以下は投稿者個人の環境での検証に基づく応急的な対処法であり、すべての環境での動作を保証するものではない。実施は自己責任で行うこと。データベースの中身に影響する操作を含むため、**まず学習用の複製データベースで試すか、講師に相談してから実施することを推奨**。

**対処法（応急処置）**
1. `xampp/mysql/data` フォルダの名前を `data2` に変更する。
2. `backup` フォルダをコピーして `data` という名前にし、それを使ってMySQLを起動する。
   - ただしこの方法だとデータベースが初期状態に戻ってしまい、それまで作成していたテーブルやデータがすべて消えてしまう問題がある。
3. 元のデータを復元するため、`data2` フォルダ内のテーブルファイルを `data` フォルダへコピーする。
   - この状態だとテーブル自体は表示されるが、中のデータが空になる（テーブル内のファイルの参照は `ibdata1` というファイルが行っているため）。
4. `data2` から `ibdata1` を `data` フォルダへコピーすると、テーブル内のデータも正しく表示されるようになる。
   - ただしこの状態ではMySQLが頻繁にクラッシュし、動作が不安定になる。
5. **最終的に安定した手順**: XAMPPを停止した状態で、ログファイル（`ib_logfile0`、`ib_logfile1`、`ibtmp1`）を削除したうえで、`data2` の `ibdata1` を `data` フォルダへコピーし、再度XAMPPを起動する。これによりテーブル構造・データともに正常表示され、MySQLも安定動作するようになった。

**関連の注意事項（トラブル予防）**
テーブル名に `5-19-2 employees` のようなスペースや記号（ハイフンなど）を含めると、後々エラーの原因になることがある。実際にエクスプローラー上でこれらの文字が内部的に変換されて保存されていることが確認できるため、トラブル回避の観点からもテーブル名にはスペースや記号を使わず、アンダースコア（`_`）などを利用することが推奨される。

*出典: 26123神林響、2026-05-27*

---

### No.4 — XAMPPを終了するたびにエラーが出る場合

**症状**
XAMPPを終了する際、毎回エラーメッセージが表示される。

**対処法（いずれかを実施）**
1. **方法1**: 管理者権限でXAMPPを実行する。
2. **方法2**: `xampp-control.ini` に書き込み権限を与える。
   - 参考: `https://fukushi-itlabo.net/2023/10/18/10560/`

上記2つの対処を行ったところ、エラーを出さずに閉じられるようになったとの報告あり。

*出典: 26123神林響、2026-05-27*

---

## 2. SpringBoot / Gradle 関連トラブル

### No.5 — SpringBootプロジェクト作成・最初のビルドに時間がかかる（トラブルではなく仕組みの理解）

**症状**
SpringBootプロジェクトの作成や、最初のビルドに非常に時間がかかる。

**原因（仕組みの説明）**
- チェックする依存関係（ライブラリ）の数が増えると、プロジェクト作成や最初のビルドに時間がかかる。これは「ファイルのサイズ」ではなく、**インターネットからダウンロードしてくるライブラリ（JARファイル）の量と、その依存関係の複雑さ**による。
- 特に画面の「Vaadin」を1つチェックするだけで、Vaadinが動くために必要な別のライブラリが何十個も自動的にダウンロードされ、チェックの数が増えるほどこの「芋づる式」のダウンロードが巨大化する。
- Java開発ツール（Maven・Gradle）は、一度ダウンロードしたライブラリを使い回す仕組みのため、**プロジェクトを新しく作った最初の1回目**や、ライブラリを初めて導入した時は、インターネットからのダウンロード待ちで特に時間がかかる。

**対処・アドバイス**
「まずはWebの基本だけ勉強したい」「練習で動かしたい」という場合は、画面を作るための`Vaadin`や、データベースをガッツリ使う`Spring Data JPA`/`JDBC`は一旦外し、`Spring Web`と`H2 Database`くらいからスモールスタートするのがおすすめ。

*出典: IA_岡野隼、2026-05-29（生成AIの回答をもとに整理・共有）*

---

### No.6 — MySQLのパスワードを間違えて登録してしまった場合

**症状**
SpringBootプロジェクトの設定などで、MySQLのパスワードを間違えて登録してしまった。

**対処法**
専用の手順書（`MySQLパスワード変更手順.txt`）を参照して変更する。

**📎 マニュアル化時の注意**: 元投稿には手順書ファイルが添付されている。PDF化の際は当該ファイルの内容を本項目に転記すること。

*出典: IA_岡野隼、2026-05-29（添付ファイル: `MySQLパスワード変更手順.txt`）*

---

### No.7 — build.gradleのバージョンエラー

**症状**
`build.gradle` のバージョンに関するエラーが発生する（SpringBootのバージョン指定に起因）。

**対処法**
1. `settings.gradle` に以下のコードを貼り付けて上書き保存する。
   ```
   pluginManagement {
       repositories {
           maven { url 'https://repo.spring.io/release' }
           mavenCentral()
           gradlePluginPortal()
       }
   }
   ```
2. `settings.gradle` を右クリック。
3. 「Gradle」→「Gradleプロジェクトのリフレッシュ」を実行する。

**補足**: この対応時点でのプロジェクトの `springframework.boot` バージョンは `4.0.6`。バージョン指定を変更する場合はこの点も踏まえて対応する。

*出典: 宇賀神歩、2026-06-17（エラーが出ていたメンバー全員がこの方法で解消済み）*

---

### No.8 — JDBCドライバ（MySQL Connector）がビルドパスから消える／`No suitable driver found`エラー

**症状**
データベース接続処理を実行すると、以下のエラーが発生する。
```
java.sql.SQLException: No suitable driver found for jdbc:mysql://localhost/<データベース名>
```
外部JARファイル（MySQLのJDBCドライバ）を「ビルド・パスの構成」からライブラリのクラスパスへ追加していたはずだが、再度「ビルド・パスの構成」を確認すると、追加したはずのJARファイルがクラスパスから削除されている状態になっている。

**対処法**
1. 外部JARとして追加するのではなく、プロジェクト内に `lib` フォルダを作成する。
2. その `lib` フォルダに `mysql-connector-j` のJARファイルをコピーする。
3. 「JARの追加」からビルドパスに追加する。
4. 「参照ライブラリー」にJARが表示されているか確認し、再度実行してみる。

**関連の恒久対策（No.9も参照）**: この問題を繰り返さないための別のアプローチとして、Gradleの `dependencies` に `runtimeOnly 'com.mysql:mysql-connector-j'` を追記する方法がある（No.9参照）。外部JARを手動配置する方式は消えやすいため、可能であればNo.9の方法に統一することを推奨。

*出典: IA_金谷優太、2026-06-18（3班で発生した事例）*

---

### No.9 — GradleでMySQL Connectorがビルドパスから消えない設定にする（No.8の恒久対策）

**症状**
No.8のように、外部JARとして手動追加したMySQL ConnectorがGradleのリフレッシュ等のタイミングでビルドパスから消えてしまう。

**対処法**
1. `build.gradle` 内の `dependencies {}` 内に以下の1行を追加する。
   ```
   runtimeOnly 'com.mysql:mysql-connector-j'
   ```
2. 保存後、「Gradle」→「リフレッシュ」を実行する。
   - これにより自動的に最新のJARファイルがインポートされ、ビルドパスに追加された状態が維持され、消えなくなる。

**⚠️ チーム運用上の注意（2点）**
1. GitHubで（他メンバーの環境で）エラーが起きないよう、**チーム全員がこの同じ設定にする**こと。
2. `build.gradle` 内にSQLコネクターに関する記述が重複しないよう注意すること（手動で追加したJARの記述とこの1行が両方残っていないか確認）。

*出典: IA_丸本善利、2026-06-24*

---

### No.10 — gitクローン後にインポートしても、Gradleプロジェクトとして認識されない

**症状**
GitHubからプロジェクトをクローンした後、Eclipseにインポートしても「Gradleプロジェクト」として正しく認識されない。

**原因**
クローンしたフォルダの一番上（例: `rensyuu01`）の直下に `build.gradle` がなく、**その一つ下の階層のフォルダ（例: `springBoot-demo2`）の中に `build.gradle` がある**ケースで起きる。Eclipseは `build.gradle` がある場所を「プロジェクトのルート（根っこ）」として認識する必要があるため、フォルダ構成を正しく指定してインポートし直す必要がある。

**対処法**
1. **一度、Eclipse上の表示を削除する**
   1. 該当プロジェクト（例: `rensyuu01 [rensyuu01 master]`）を右クリック→「削除」を選択。
   2. 「ディスク上からプロジェクト・コンテンツを削除」のチェックが**外れていること**を必ず確認してから「OK」を押す（チェックを入れるとクローンしたファイル自体が消えるため注意）。
2. **「一つ下のフォルダ」を指定してGradleインポートし直す**
   1. メニューの「ファイル」→「インポート...」をクリック。
   2. 「Gradle」→「既存のGradleプロジェクト」を選択して「次へ」。
   3. ウェルカム画面が出たら「次へ」。
   4. 「プロジェクトのルート・ディレクトリー」の「参照(B)...」ボタンを押す。
   5. 先ほど削除したフォルダの**中にある `build.gradle` が入っているフォルダ**（例: `rensyuu01` の中の `springBoot-demo2`）を選択する。
      - パスの例: `C:\pleiades\2025-12\workspace\rensyuu01\springBoot-demo2`

*出典: IA_岡野隼、2026-06-18（画像付き解説あり）*

---

## 3. Git / GitHub 関連トラブル

### No.11 — GitHubでpullが正常に完了しない（Checkout conflict）

**症状**
GitリポジトリからPullすると、以下のようなエラーが表示され、Pullが強制終了する。
```
1 リポジトリーのプル中
Checkout conflict with files:
target/tab.launch
target/vaadin-dev-server-settings.json
```

**原因**
Vaadinの実行時に自動生成される `target` フォルダ内のファイルが原因で、Pull時に上書きができず処理が停止してしまうため。`target` フォルダは基本的に自動生成されるものなので、削除しても問題ない。

**対処法**
1. Eclipseを閉じる。
2. Windowsのエクスプローラーから `target` フォルダを削除する。
   - 手順: ローカルディスク → ユーザー → 自分の名前 → git → チーム名が書いてあるファイル → `target` フォルダ削除
3. 再度Pullを実行する。
4. これで解決しない場合は講師へ相談する。

**📎 マニュアル化時の備考**: 同様の症状（`Checkout conflict with files: build.gradle`）が別ファイルで発生した報告もあり。原因が同種（自動生成／変更されたファイルの競合）であれば同じ手順（該当ファイルの削除→再Pull）で対応できる可能性が高いが、`build.gradle` は自動生成ファイルではないため、削除前に内容のバックアップを取ることを推奨。

*出典: 26128小島わかな、2026-06-23*

---

## 4. ブラウザ表示関連トラブル

### No.12 — SpringBootのブラウザ表示がうまくいかない

**症状**
SpringBootを起動し、Google Chromeで `localhost:8080` にアクセスしても画面が表示されない。

**対処法**
Google ChromeではなくMicrosoft Edgeからアクセスすると、表示が正常に行われる。

*出典: 辛川友斗、2026-06-19*

---

## 5. 開発効率化のヒント（トラブル対応ではないが有用な情報）

### No.13 — Windows操作の効率化ショートカット

日々の開発作業を効率化するショートカットキー。

| キー操作 | 効果 |
|---|---|
| `Ctrl + X` | 切り取り（カット） |
| `Alt + Tab` | ウィンドウ切り替え |
| `Win + D` | 全ウィンドウ最小化 |
| `Tab` / `Shift + Tab` | フォーカスの移動 |

*出典: IA_岡野隼、2026-05-20*

### No.14 — シーケンス図を文字化して修正しやすくする（Mermaid Live Editor）

シーケンス図を文字（テキスト）ベースで記述し、ブラウザ上で確認・修正できる無料サービス「Mermaid Live Editor」の活用方法を紹介。図の手直しがしやすくなる。

*出典: IA_岡野隼、2026-05-26*

### No.15 — SQL練習サイトの紹介

ブラウザ上でSQLの練習ができるサイト。研修用PCではブロックされる可能性があるため、時間に余裕がある際に自宅PC等での活用を推奨。

*出典: IA_岡野隼、2026-06-09*

---

## 6. 未解決・要フォローアップ事項

### 要フォロー-1 — SpringBootのエラーがbuild.gradleのバージョン修正後も解消しない（原因不明）

**症状**
以前共有された対処法（No.7のbuild.gradleバージョンエラー対応）を実施したところ、その部分のエラーは解消したが、その**後もSpringBootのエラー表示が消えない**状態。他のフォルダ等も一通り確認したが、エラー箇所を特定できていない。Eclipseの再起動を数回行っても状況は変わらず。チームの開発演習プロジェクトのインポートもエラーになり、何も開けない状態。ワークスペース1・ワークスペース2の両方で同様の事象が発生。

**現状**: この投稿に対する解決策の共有・返信はSlack上で確認できておらず、**本チャンネル内では未解決のまま**となっている。

**申し送り事項**: 来年同様の事象が起きた場合に備え、①どのバージョン指定が影響したか、②Eclipseのワークスペース単位でどこまで再現するか、③最終的にどう解決したか（または個別対応で終わったか）を、解決した際に本マニュアルへ追記すること。

*出典: 26208井上未帆、2026-06-22*

---

## 付記・注意（マニュアル編集時に削除可）

- 本ファイルはSlackの投稿を時系列に沿って抽出し、症状・原因・対処法の形式に再構成したドラフトです。実機での再現性確認や表現統一を行ったうえで正式なマニュアルとして配布・PDF化することを想定しています。
- 各項目の「出典」は内容の裏付け確認用のメモです。マニュアル確定版では削除して構いません。
- スレッド返信があった項目（No.7）は、返信内容も本文に統合済みです。
- 画像・添付ファイル付きの投稿（No.6、No.10）は、当該ファイルの中身を本ファイルには転記していません。PDF化の際は元のSlack投稿から画像・ファイルを取得し、該当項目に追加してください。
